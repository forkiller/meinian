<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.atguigu.dao.OrderDao">
    <insert id="add">
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
            select last_insert_id()
        </selectKey>
        insert into t_order (member_id, orderDate, orderType, orderStatus, setmeal_id)
        values (#{memberId}, #{orderDate}, #{orderType}, #{orderStatus}, #{setmealId});
    </insert>


    <!--Order order = new Order(#{emberId}, #{ate}, #{ull}, #{ull}, setmealId);-->
    <select id="getOrdersByCondition" resultType="com.atguigu.pojo.Order">
        select *
        from t_order
        <where>
            <if test="memberId != null">
                and member_id=#{memberId}
            </if>
            <if test="orderDate != null">
                and orderDate=#{orderDate}
            </if>
            <if test="orderType != null">
                and orderType=#{orderType}
            </if>
            <if test="orderStatus != null">
                and orderStatus=#{orderStatus}
            </if>
            <if test="setmealId != null">
                and setmeal_id=#{setmealId}
            </if>
        </where>
    </select>
    <!--<select id="findById" resultType="java.util.Map">
        select m.name as member, s.name as setmeal, o.orderDate, o.orderType
        from t_order o,
             t_member m,
             t_setmeal s
        where o.member_id = m.id
          and o.setmeal_id = s.id
          and o.id = #{id}
    </select>-->
    <select id="findById" parameterType="int" resultType="map">
        select m.name member, s.name setmeal, o.orderDate orderDate, o.orderType orderType
        from t_order o,
             t_member m,
             t_setmeal s
        where o.member_id = m.id
          and o.setmeal_id = s.id
          and o.id = #{id}
    </select>
    <select id="getTodayOrderNumber" resultType="java.lang.Integer">
        select count(1)
        from t_order o
        where o.orderDate = #{today};
    </select>
    <select id="getTodayVisitsNumber" resultType="java.lang.Integer">
        select count(1)
        from t_order o
        where o.orderDate = #{today}
          and o.orderStatus = '已出游';
    </select>
    <select id="getThisWeekVisitNumber" resultType="java.lang.Integer">
        select count(1)
        from t_order o
        where o.orderStatus = '已出游'
          and o.orderDate between #{begin} and #{end};
    </select>
    <select id="getThisMonthOrderNumber" resultType="java.lang.Integer">
        select count(1)
        from t_order o
        where o.orderDate between #{begin} and #{end};
    </select>
    <select id="getThisMonthVisitNumber" resultType="java.lang.Integer">
        select count(1)
        from t_order o
        where o.orderStatus = '已出游'
          and o.orderDate between #{begin} and #{end};
    </select>

    <select id="findHotSetmeal" resultType="java.util.Map">
        select s.name name, count(o.id) setmeal_count,count(o.id)/(select count(id) from t_order) proportion
        from t_setmeal s,
             t_order o
        where s.id = o.setmeal_id
        group by s.name
        order by setmeal_count desc
        limit 0,4;
    </select>
    <select id="getThisWeekOrderNumber" resultType="java.lang.Integer">
        select count(1)
        from t_order o
        where o.orderDate between #{begin} and #{end};
    </select>

</mapper>